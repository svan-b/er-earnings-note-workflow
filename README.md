```mermaid
```

5. **Delete that line and the closing ``` line**, then **retype them by hand using your keyboard backtick key** (the key left of the number 1, below Escape)

6. Click **Preview** at the top of the editor to see if the diagram renders

Before you commit anything, just click Preview after retyping one fence block. If the preview shows a diagram, the problem was invisible characters in the pasted backticks. If the preview still shows raw code, screenshot it and I'll look at the actual raw source differently.

The backtick key on your keyboard: **` (left of 1, below Esc)**. Three of those, then the word `mermaid`, no space between.
